(()=>{const $=s=>document.querySelector(s);const val=(f,k)=>String(new FormData(f).get(k)||'');const esc=s=>String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));function validUrl(s){try{const u=new URL(s);return /^https?:$/.test(u.protocol)}catch{return false}}function map(stack,pain,model,time){let path='Keep the stack lightweight for now';let why='Your current facts do not yet justify adding ERP weight.';let risk='Do not buy a larger system before the operating constraint is isolated.';let pilot='Test one quote/order through purchasing, material issue, production status and actual cost.';let missing='How many BOM levels and production steps must the system plan?';if(stack==='quickbooks'||stack==='spreadsheets'){if(pain==='inventory'){path='Inventory-first system, with manufacturing kept light';why='The first boundary appears to be purchasing, stock visibility and shortage control rather than deep production planning.';risk='Adding a full ERP can create migration and process overhead before the inventory problem is solved.';missing='Do you need multi-level BOM planning or mainly reliable stock/purchasing?'}if(['planning','costing','traceability'].includes(pain)||['mto','batch','complex'].includes(model)){path='Light-to-full MRP evaluation';why='The operating problem has crossed from accounting/inventory into production execution, planning, traceability or actual-cost control.';risk='A light inventory app may improve stock visibility but still leave scheduling, WIP or costing outside the system.';missing='Do routings/capacity or multi-level BOM explosions materially drive purchasing and delivery dates?'}}if(stack==='inventory'){path=['planning','costing','traceability'].includes(pain)?'MRP evaluation':'Strengthen the inventory layer before ERP';why=path==='MRP evaluation'?'The constraint sits downstream of inventory visibility and now requires production control.':'The current evidence still points to inventory discipline rather than full manufacturing planning.';risk=path==='MRP evaluation'?'Do not assume the current inventory tool can become a production system through workarounds.':'Avoid replacing the whole stack if purchasing, locations and replenishment are the real problem.';missing='Which production decision is still being managed outside the inventory system?'}if(stack==='mrp'){path=pain==='stabilization'||pain==='integration'?'Stabilize and integrate the current MRP before replacement':'Test current MRP gaps before considering deeper ERP';why='A system already exists, so replacement should require evidence that the remaining problem is structural rather than implementation, data or process ownership.';risk='Replacing a workable MRP can reset master data and adoption without fixing ownership problems.';missing='Which specific workflow still requires spreadsheets, manual re-entry or offline decisions?'}if(stack==='erp'){path='ERP implementation / process-control review before replacement';why='The default hypothesis is not "buy another ERP". The first question is whether rollout, master data, integration, adoption or process ownership is creating the gap.';risk='A replacement project can reproduce the same governance failure at higher cost.';missing='Is the failure primarily configuration, data, integration, adoption or an actual product capability gap?'}if(model==='complex'&&['spreadsheets','quickbooks','inventory'].includes(stack)){path='Deeper MRP / ERP evaluation';why='Multi-site or complex manufacturing raises the coordination burden across planning, inventory, costing and master data.';risk='A lightweight layer may become a new reconciliation point rather than the system of record.'}const urgency=time==='now'?'Active 0–30 day decision':time==='soon'?'Active 31–90 day evaluation':'Research stage';return{path,why,risk,pilot,missing,urgency}}function reviewHref(host,x,f){const lines=['MFG Decision Map review request',`Company: ${host}`,`Current stack: ${val(f,'stack')}`,`Primary constraint: ${val(f,'pain')}`,`Manufacturing model: ${val(f,'model')}`,`Decision timing: ${val(f,'time')}`,`Initial path: ${x.path}`,`Missing fact: ${x.missing}`,'','Please review this initial map and send me a concise 3-point fit/non-fit note.'];return 'mailto:liuambition982+mfgstacklab@gmail.com?subject='+encodeURIComponent(`Decision Map review | ${host}`)+'&body='+encodeURIComponent(lines.join('\n'))}function render(){const f=$('#de-form'),r=$('#de-result'),url=$('#company-url').value.trim();const required=['stack','pain','model','time'];if(!validUrl(url)||required.some(k=>!val(f,k))){r.style.display='block';r.innerHTML='<b>Enter a valid company URL and answer all four operating questions.</b>';return}const x=map(val(f,'stack'),val(f,'pain'),val(f,'model'),val(f,'time'));let host='the company';try{host=new URL(url).hostname.replace(/^www\./,'')}catch{}const review=reviewHref(host,x,f);r.style.display='block';r.innerHTML=`<span class="tag">${esc(x.urgency)}</span><h2>${esc(x.path)}</h2><p><b>Company anchor:</b> ${esc(host)}</p><p><b>First decision boundary:</b> ${esc(x.why)}</p><p><b>Do not over-buy:</b> ${esc(x.risk)}</p><p><b>Best next pilot:</b> ${esc(x.pilot)}</p><p><b>One missing fact that could change the recommendation:</b> ${esc(x.missing)}</p><div class="actions"><a class="btn btn-primary" href="${review}">Request a free 3-point review</a><a class="btn btn-secondary" href="fit-check.html?utm_source=decision-engine-v2&utm_campaign=initial-map">Run the deeper fit check</a></div><p class="fine">The review request opens your email app with this map prefilled. Nothing is transmitted unless you choose to send the email. Evidence boundary: this initial map uses only the facts entered in this browser. No company website content was fetched.</p>`;r.scrollIntoView({behavior:'smooth',block:'start'});window.dispatchEvent(new CustomEvent('mfg:v2',{detail:{event:'MAP_COMPLETED_LOCAL',host,path:x.path,time:val(f,'time')}}))}document.addEventListener('DOMContentLoaded',()=>{$('#de-run')?.addEventListener('click',render);window.dispatchEvent(new CustomEvent('mfg:v2',{detail:{event:'LANDING_VIEW',page:'decision-engine-v2'}}))})})();